{% docs visitid_description %}

Visit Id is identifier of the session in Google Analytics.
It is found in many models and can be a join key

Visit Id is identifier of the session in Google Analytics.
It is found in many models and can be a join key

Visit Id is identifier of the session in Google Analytics.
It is found in many models and can be a join key

Visit Id is identifier of the session in Google Analytics.
It is found in many models and can be a join key


{% enddocs %}